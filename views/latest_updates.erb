var target = $('#updates');

<% for update in @updates %>
  var update = '<%= js_escape_html partial(:_update, :locals => {:update => update, :new => true}) %>';
  target.prepend(update);
<% end %>

extLinksInNewTab(); // do it again for links in new updates

$('.update.new').slideDown(500);

$('#status').fadeOut(500);

